{
  "name": "probo_clone",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cd web_socket && npx tsc -b && node dist/index.js && cd ../server && npx tsc -b && node dist/index.js && ../worker && npx tsc -b && node dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "probo_clone": "file:"
  }
}
